TABLE("Map name","Bonus rank","Map ID","State Mask", "Category ID"){
	headerFormat{
		uint(4,ROW_SIZE):2;
	}
	
	rowFormat{
		shiftJis(34);
		zero(1);
		uint(1);
		uint(2);
		uint(3b);
		uint(13b);
	}
}